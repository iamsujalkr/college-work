// Program to calculate area of square
# include<stdio.h>

int main() {

    // Taking side from user 
    float side;
    printf("Enter the side of square:  ");
    scanf("%f",&side);

    // Printing the area of square 
    printf("The area of square is %f square units",side*side);
    return 0;
}